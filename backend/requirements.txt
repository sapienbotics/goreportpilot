# Core framework — pinned for reproducibility
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.3
pydantic-settings==2.2.1
python-multipart==0.0.9
python-dotenv==1.0.1

# Supabase — 2.7.4 resolves to httpx>=0.24,<0.28 (no conflict with fastapi/openai)
# httpx is intentionally NOT pinned here; pip resolves a compatible version automatically
supabase==2.7.4

# AI & report generation — pinned
openai==1.14.0
python-pptx==0.6.23
reportlab==4.1.0
matplotlib==3.8.3
cryptography==42.0.5

# Image handling — minimum version, pip picks compatible
pillow>=10.2.0

# Google APIs — minimum versions, pip picks compatible
google-analytics-data>=0.18.5
google-ads>=24.1.0
google-auth>=2.28.1
google-auth-httplib2>=0.2.0

# Meta / Facebook Ads SDK
facebook-business>=19.0.0

# Email & payments
resend>=0.8.0
stripe>=8.4.0
